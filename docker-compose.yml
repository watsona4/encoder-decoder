services:
  b64drive:
    build: .
    ports:
      - "8227:8000"
    env_file:
      - .env
    environment:
      REDIRECT_URI: https://decode.battenkillwoodworks.com/oauth/callback
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/status"]
      interval: 10s
      timeout: 5s
      retries: 10
    volumes:
      - ./token.json:/run/secrets/sa.json:ro
